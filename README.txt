This program was designed to read EGI RAW files and generate text files.  This was particularly necessary since you needed a hasp to export your data to text. 

This program uses a very consistent naming scheme, which some users like more than the default Net Station "Export to Text" format.
